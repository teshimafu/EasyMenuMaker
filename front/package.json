{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "nuxt": "^3.4.1",
    "prettier": "^2.8.7"
  },
  "dependencies": {
    "@nuxt/types": "^2.16.3",
    "axios": "^1.3.5",
    "dotenv": "^16.0.3"
  }
}
